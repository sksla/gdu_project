<?xml version="1.0" encoding="UTF-8"?>
<configuration>

   <appender class="ch.qos.logback.core.ConsoleAppender" name="consoleLog">
      <encoder>
         <pattern>%-5level: [%date{HH:mm:ss}] [%logger{0}:%line] - %msg%n</pattern>
      </encoder>
   </appender>
   
   <!-- 웹소캣용 어펜더 -->
   <appender class="ch.qos.logback.core.ConsoleAppender" name="websocketLog">
      <encoder>
         <pattern>[%date{HH:mm:ss}] - %msg%n</pattern>
      </encoder>
   </appender>
   
		<!-- logger -->
   <logger name="org.springframework" level="info"/>
   <logger name="com.cu.gdu" level="debug"/>
   <!-- additivity false : root 로거로 전달되지 않음, 참조할 어펜더를 등록 해줘야함 -->
   <logger name="com.cu.gdu.handler" level="debug" additivity="false">
   	<appender-ref ref="websocketLog"/>
   </logger>
   
   <logger name="jdbc.sqlonly" 				level="off"/> <!-- 쿼리문만 -->
   <logger name="jdbc.sqltiming" 			level="info"/> <!-- 쿼리문 + 실행시간 -->
   <logger name="jdbc.audit" 					level="off"/> <!-- jdbc 호출정보 -->
   <logger name="jdbc.resultset" 			level="off"/> <!-- ResultSet 호출정보 -->
   <logger name="jdbc.resultsettable" level="off"/> <!-- ResultSet 결과 -->
   <logger name="jdbc.connection" 		level="off"/> <!-- Connection 호출정보 -->
   
   <root level="warn">
      <appender-ref ref="consoleLog"/>
   </root>
</configuration>